---
description: Bootstrap using project engineering requirements
---

Read AI_CONTEXT.md first.

## User request

$ARGUMENTS

## Procedure

When local cache writes are authorised, run python3 ai_workflow/tools/repo_bootstrap.py prepare --pretty. Read all returned JSON. Follow ai_workflow/bootstrap_analysis.md, inspect changed and affected code and write requested knowledge outputs. Run complete only after the outputs exist. If required mode fails, stop dependent work. Auto and disabled permit source fallback. Do not treat configured adapter commands as executed.
